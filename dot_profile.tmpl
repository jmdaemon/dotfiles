# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.
# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="/$HOME/bin:$PATH"
fi

# https://wiki.archlinux.org/index.php/XDG_Base_Directory 
XDG_CONFIG_HOME="$HOME/.config"
XDG_DATA_HOME="$HOME/.local/share"
XDG_CACHE_HOME="$HOME/.cache"

# History 
HISTORY_DIR="$HOME/.history"
export GDBHISTFILE=$HISTORY_DIR/gdb/"{{ .system }}"/gdb_history
export PYTHONHISTFILE=$HISTORY_DIR/python/"{{ .system }}"/python_history
export OCTAVE_HISTFILE=$HISTORY_DIR/octave/"{{ .system }}"/octave_history
export LESSHISTFILE="$HISTORY_DIR/less/{{ .system }}"/less_history
export NODE_REPL_HISTORY="$HISTORY_DIR"/nodejs/"{{ .system }}"/node_repl_history
export SQLITE_HISTORY="$HISTORY_DIR"/sqlite3/"{{ .system }}"/sqlite_history

# RC Files
export OCTAVE_SITE_INITFILE="$XDG_CONFIG_HOME/octave/octaverc"
export NPM_CONFIG_USERCONFIG=$XDG_CONFIG_HOME/npm/npmrc
export CONDARC="$XDG_CONFIG_HOME/conda/condarc"
export WGETRC="$XDG_CONFIG_HOME/wget/wgetrc"

# Languages
export CARGO_HOME="$XDG_DATA_HOME"/cargo 
export GOPATH="$XDG_DATA_HOME"/go 
export GEM_HOME="$XDG_DATA_HOME"/gem
export GEM_SPEC_CACHE="$XDG_CACHE_HOME"/gem   
export RUSTUP_HOME="$XDG_DATA_HOME"/rustup 
export _JAVA_OPTIONS=-Djava.util.prefs.userRoot="$XDG_CONFIG_HOME"/java


export NVM_DIR="$XDG_DATA_HOME"/nvm 
export NPM_DIR="$XDG_DATA_HOME"/npm

export NPM_CONFIG_CACHE=$XDG_CACHE_HOME/npm
export NPM_CONFIG_TMP=$XDG_RUNTIME_DIR_HOME/npm
export NODE_PATH="$NODE_PATH:$NPM_DIR/npm/lib/node_modules"



#export BASH_COMPLETION_USER_FILE="$XDG_CONFIG_HOME"/bash-completion/bash_completion

# Applications - Config
export CUDA_CACHE_PATH="$XDG_CACHE_HOME"/nv
export GTK_RC_FILES="$XDG_CONFIG_HOME"/gtk-1.0/gtkrc
export GTK2_RC_FILES="$XDG_CONFIG_HOME"/gtk-2.0/gtkrc 
export DOCKER_CONFIG="$XDG_CONFIG_HOME"/docker
export IPYTHONDIR="$XDG_CONFIG_HOME"/jupyter
export JUPYTER_CONFIG_DIR="$XDG_CONFIG_HOME"/jupyter 
export XMONAD_CONFIG_HOME="$XDG_CONFIG_HOME"/xmonad
export SPACEMACSDIR="$XDG_CONFIG_HOME"/spacemacs
export PARALLEL_HOME="$XDG_CONFIG_HOME"/parallel 
export KAGGLE_CONFIG_DIR="$XDG_CONFIG_HOME"/kaggle
export ANDROID_PREFS_ROOT="$XDG_CONFIG_HOME"/android
export MOZC_CONFIGURATION_DIRECTORY="$XDG_CONFIG_HOME"/mozc
export ncmpcpp_directory="$XDG_CONFIG_HOME"/ncmpcpp
export WEECHAT_HOME=$XDG_CONFIG_HOME/weechat/

# Applications - Data
export MACHINE_STORAGE_PATH="$XDG_DATA_HOME"/docker-machine 
export WINEPREFIX="$XDG_DATA_HOME"/wineprefixes/default 
export GRADLE_USER_HOME="$XDG_DATA_HOME"/gradle
export VSCODE_PORTABLE="$XDG_DATA_HOME"/vscode
export SSB_HOME="$XDG_DATA_HOME"/zoom
export GNUPGHOME="$XDG_DATA_HOME"/gnupg
export CARGO_HOME="$XDG_DATA_HOME"/cargo

# QT Theming
export QT_QPA_PLATFORMTHEME=qt5ct
export QT_AUTO_SCREEN_SCALE_FACTOR=1
export FONTCONFIG_PATH=/etc/fonts

if which ruby >/dev/null && which gem >/dev/null; then
    PATH="$(ruby -r rubygems -e 'puts Gem.user_dir')/bin:$PATH"
fi

SBIN=/usr/sbin
NPM=$NPM_DIR/bin
RUST="$CARGO_HOME"/bin
GO=/usr/local/go/bin
LOCAL=$HOME/.local/bin
#RVM=$HOME/.rvm/bin # Make sure this is the last PATH variable change.
#export PATH=$PATH:$SBIN:$NPM:$RUST:$GO:$LOCAL:$RVM
export PATH=$PATH:$SBIN:$NPM:$RUST:$GO:$LOCAL

# Java
OPENJDK_8=/usr/lib/jvm/java-1.8.0-openjdk-amd64
export JAVA_HOME=/usr/lib/jvm/java-15-openjdk

#export LIBVIRT_DEFAULT_URI='qemu:///system'

export EDITOR=nvim

ALIAS="$XDG_CONFIG_HOME/dotfiles/scripts/aliases" 
source $ALIAS/aliases.sh
{{- if ( eq .system "ubuntu-workstation" ) -}}source $ALIAS/nvidia.sh{{ end }}
{{ if ( eq .system "arch-laptop" ) -}}source $ALIAS/pacman.sh{{ end }}
#source $XDG_CONFIG_HOME/dotfiles/scripts/startup/nvm.sh
#source $XDG_CONFIG_HOME/dotfiles/scripts/terminal/fortunecow.sh

#export GOPATH="$HOME/workspace/go_projects"
#export GOBIN="$GOPATH/bin"

# [[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

# Persistent Tmux Sessions
#/usr/bin/tmux attach -t 0
